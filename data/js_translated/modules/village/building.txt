{
    营火: {cap: 1, 保留: true, icon: 'mdi-campfire', note: 'village_1', price() {
        return {木头: 5, 石头: 5};
    }, timeNeeded() {
        return 5;
    }, effect: [
        {name: '层 1 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    棚子: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-tent', note: 'village_2', requirement() {
        return store.state.解锁.层 1 建筑.use;
    }, price(lvl) {
        return {植物纤维: Math.pow(1.35, lvl) * 15, 木头: Math.pow(1.32, lvl) * 10};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * 10);
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl}
    ]},
    农场: {cap: 10, capMult: true, 保留: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-tractor', note: 'village_3', requirement() {
        return store.state.解锁.层 1 建筑.use;
    }, price(lvl) {
        return {木头: Math.pow(1.65, lvl) * 200, 石头: Math.pow(1.65, lvl) * 400};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 40);
    }, effect: [
        {name: 'farmer', type: 'villageJob', value: lvl => lvl},
        {name: '植物纤维容量', type: '+', value: lvl => lvl > 1 ? (250 * (lvl - 1)) : null}
    ]},
    种植园: {cap: 10, capMult: true, 保留: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-forest', note: 'village_4', requirement() {
        return store.state.解锁.层 1 建筑.use;
    }, price(lvl) {
        return {植物纤维: Math.pow(1.65, lvl) * 750, 石头: Math.pow(1.65, lvl) * 430};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 50);
    }, effect: [
        {name: '收割机', type: 'villageJob', value: lvl => lvl},
        {name: '木头容量', type: '+', value: lvl => lvl > 1 ? (250 * (lvl - 1)) : null}
    ]},
    矿井: {cap: 10, capMult: true, 保留: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-tunnel', note: 'village_5', requirement() {
        return store.state.解锁.层 1 建筑.use;
    }, price(lvl) {
        return {木头: Math.pow(1.65, lvl) * 1150};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 60);
    }, effect: [
        {name: 'miner', type: 'villageJob', value: lvl => lvl},
        {name: '石头容量', type: '+', value: lvl => lvl > 1 ? (250 * (lvl - 1)) : null}
    ]},
    社区中心: {cap: 1, 保留: true, icon: 'mdi-home-account', note: 'village_6', requirement() {
        return store.state.解锁.层 1 建筑.use;
    }, timeNeeded() {
        return 750;
    }, price() {
        return {木头: 1800, 石头: 1650, 金属: 100};
    }, effect: [
        {name: '层 2 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    小房子: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-home-variant', note: 'village_7', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        return {木头: Math.pow(1.35, lvl) * 2750, 金属: Math.pow(1.35, lvl) * 250, 水: Math.pow(1.5, lvl) * 400};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * 210);
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl}
    ]},
    起重机: {cap: 20, icon: 'mdi-crane', note: 'village_31', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.2, lvl) * 45);
    }, price(lvl) {
        return {木头: Math.pow(1.5, lvl) * 580, 金属: Math.pow(1.35, lvl) * 275};
    }, effect: [
        {name: '建造速度', type: '+', value: lvl => lvl}
    ]},
    财政部: {cap: 10, hasDescription: true, icon: 'mdi-treasure-chest', note: 'village_9', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        let obj = {植物纤维: Math.pow(1.25, Math.max(0, lvl - 9)) * Math.pow(1.5, lvl) * 2600};
        if (lvl <= 0) {
            obj.水果 = 325;
            obj.粮食 = 550;
        }
        return obj;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.4, lvl) * 240);
    }, effect: [
        {name: '金币升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '税率', type: '+', value: lvl => splicedLinear(0.025, 0.01, 10, lvl)}
    ]},
    存储: {cap: 20, icon: 'mdi-database', note: 'village_8', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        let obj = {植物纤维: Math.pow(lvl * 0.02 + 1.15, lvl) * 900, 木头: Math.pow(lvl * 0.02 + 1.15, lvl) * 900, 石头: Math.pow(lvl * 0.02 + 1.18, lvl) * 1400};
        if (lvl <= 0) {
            obj.金币 = 50;
        }
        return obj;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.15, lvl) * 225);
    }, effect: [
        {name: '基础材料容量', type: '*', value: lvl => Math.pow(1.2, lvl)},
        {name: '金属容量', type: '*', value: lvl => lvl > 5 ? Math.pow(1.2, lvl - 5) : null}
    ]},
    锻造: {cap: 20, icon: 'mdi-anvil', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        return {石头: Math.pow(lvl * 0.02 + 1.25, lvl) * 2750, 金属: Math.pow(lvl * 0.02 + 1.18, lvl) * 250};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 180);
    }, effect: [
        {name: '金属增益', type: '*', value: lvl => lvl * 0.1 + 1},
        {name: '金属容量', type: '+', value: lvl => lvl * 200}
    ]},
    保险箱: {cap: 20, icon: 'mdi-safe', note: 'village_10', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        return {金属: Math.pow(lvl * 0.02 + 1.2, lvl) * 900, 金币: Math.pow(lvl * 0.02 + 1.18, lvl) * 150};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 270);
    }, effect: [
        {name: '金币容量', type: '+', value: lvl => lvl * 100},
        {name: '金币容量', type: '*', value: lvl => Math.pow(1.2, lvl)}
    ]},
    水井: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-water-well', note: 'village_11', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        return {植物纤维: Math.pow(1.65, lvl) * 6800, 木头: Math.pow(1.65, lvl) * 4500, 石头: Math.pow(1.65, lvl) * 5000};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 300);
    }, effect: [
        {name: 'wellWorker', type: 'villageJob', value: lvl => lvl},
        {name: '水容量', type: '+', value: lvl => lvl > 1 ? (1000 * Math.min(lvl - 1, 9)) : null},
        {name: '水容量', type: '*', value: lvl => lvl > 1 ? Math.pow(1.5, Math.min(lvl - 1, 9)) : null}
    ]},
    花园: {cap: 20, icon: 'mdi-flower', note: 'village_12', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, price(lvl) {
        return {植物纤维: Math.pow(1.25, lvl) * 8750, 水: Math.pow(1.33, lvl) * 500};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 480);
    }, effect: [
        {name: '植物纤维容量', type: '*', value: lvl => lvl * 0.15 + 1},
        {name: '金币容量', type: '+', value: lvl => lvl * 50}
    ]},
    市政厅: {cap: 1, 保留: true, icon: 'mdi-town-hall', note: 'village_13', requirement() {
        return store.state.解锁.层 2 建筑.use;
    }, timeNeeded() {
        return buildNum(14.4, 'K');
    }, price() {
        return {木头: buildNum(12.8, 'K'), 石头: buildNum(10.5, 'K'), 金属: 3150, 水: 2900};
    }, effect: [
        {name: '层 3 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    房子: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-home', note: 'village_14', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.35, lvl) * buildNum(17.8, 'K'),
            木头: Math.pow(1.35, lvl) * buildNum(16, 'K'),
            金属: Math.pow(1.35, lvl) * 2600,
            知识: lvl * 5 + 75
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * 900);
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl}
    ]},
    棚子: {icon: 'mdi-home-analytics', cap: 5, requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.75, lvl) * 1600);
    }, price(lvl) {
        return {木头: Math.pow(1.8, lvl) * buildNum(14.5, 'K'), 石头: Math.pow(2.05, lvl) * 9000, 金属: Math.pow(1.7, lvl) * 5200};
    }, effect: [
        {name: '水容量', type: '*', value: lvl => lvl * 0.2 + 1},
        {name: '镰刀升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '斧头升级', type: '解锁', value: lvl => lvl >= 2},
        {name: '镐子升级', type: '解锁', value: lvl => lvl >= 3},
        {name: '浇水器可以升级', type: '解锁', value: lvl => lvl >= 4},
        {name: '投资升级', type: '解锁', value: lvl => lvl >= 5}
    ]},
    隧道: {icon: 'mdi-tunnel', cap: 15, requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 1350);
    }, price(lvl) {
        return {植物纤维: Math.pow(1.35, lvl) * buildNum(12, 'K'), 水: Math.pow(1.5, lvl) * 850};
    }, effect: [
        {name: '石头增益', type: '*', value: lvl => lvl * 0.1 + 1},
        {name: '石头容量', type: '*', value: lvl => lvl * 0.2 + 1}
    ]},
    锯木厂: {icon: 'mdi-saw-blade', cap: 15, requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 1500);
    }, price(lvl) {
        return {金属: Math.pow(1.3, lvl) * 3200, 水: Math.pow(1.5, lvl) * 1150};
    }, effect: [
        {name: '木头增益', type: '*', value: lvl => lvl * 0.1 + 1},
        {name: '木头容量', type: '*', value: lvl => lvl * 0.2 + 1}
    ]},
    图书馆: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-book', note: 'village_15', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {木头: Math.pow(1.65, lvl) * buildNum(15, 'K'), 水: Math.pow(1.85, lvl) * 6100};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 2100);
    }, effect: [
        {name: 'librarian', type: 'villageJob', value: lvl => lvl},
        {name: '知识容量', type: '+', value: lvl => lvl > 1 ? (5 * (lvl - 1)) : null}
    ]},
    水族馆: {icon: 'mdi-fishbowl', cap: 20, note: 'village_16', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 2400);
    }, price(lvl) {
        return {水: Math.pow(1.5, lvl) * 4400, 知识: lvl * 10 + 35};
    }, effect: [
        {name: '金属容量', type: '*', value: lvl => lvl * 0.15 + 1},
        {name: '金币容量', type: '*', value: lvl => lvl * 0.15 + 1}
    ]},
    玻璃制造厂: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-glass-wine', note: 'village_17', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {金属: Math.pow(1.65, lvl) * buildNum(12, 'K'), 水: Math.pow(1.85, lvl) * buildNum(24, 'K')};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * 3000);
    }, effect: [
        {name: 'glassblower', type: 'villageJob', value: lvl => lvl},
        {name: '玻璃容量', type: '+', value: lvl => lvl > 1 ? (250 * (lvl - 1)) : null}
    ]},
    知识塔: {cap: 50, icon: 'mdi-wizard-hat', note: 'village_19', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.5, lvl) * buildNum(44, 'K'),
            石头: Math.pow(1.5, lvl) * buildNum(35, 'K'),
            玻璃: Math.pow(1.5, lvl) * 450,
            知识: Math.ceil(lvl * 8 * Math.pow(1.05, lvl) + 50)
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.24, lvl) * 3300);
    }, effect: [
        {name: '玻璃容量', type: '+', value: lvl => lvl * 250},
        {name: '水容量', type: '*', value: lvl => lvl * 0.2 + 1},
        {name: '知识容量', type: '+', value: lvl => lvl * 3}
    ]},
    微型铁匠: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-fireplace', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.65, lvl) * buildNum(60, 'K'),
            石头: Math.pow(1.65, lvl) * buildNum(35, 'K'),
            玻璃: Math.pow(1.4, lvl) * 600
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * 2500);
    }, effect: [
        {name: '金属增益', type: '*', value: lvl => lvl * 0.1 + 1},
        {name: '工人', type: '+', value: lvl => lvl > 4 ? Math.floor(lvl / 5) : null}
    ]},
    教堂: {cap: 25, hasDescription: true, icon: 'mdi-church', note: 'village_18', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.65, lvl) * buildNum(65, 'K'),
            石头: Math.pow(1.65, lvl) * buildNum(85, 'K'),
            玻璃: Math.pow(1.5, lvl) * 1700
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * 4800);
    }, effect: [
        {name: '信仰增益', type: '+', value: lvl => getSequence(1, lvl) * 0.02}
    ]},
    学校: {icon: 'mdi-school', cap: 5, note: 'village_20', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(2.25, lvl) * buildNum(400, 'K'),
            金属: Math.pow(2.25, lvl) * buildNum(45, 'K'),
            玻璃: Math.pow(2.1, lvl) * 4800,
            金币: Math.pow(1.85, lvl) * buildNum(70, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.6, lvl) * 3600);
    }, effect: [
        {name: '知识容量', type: '+', value: lvl => lvl * 5},
        {name: '基础升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '加工升级', type: '解锁', value: lvl => lvl >= 2},
        {name: '水泵升级', type: '解锁', value: lvl => lvl >= 3},
        {name: '沙子升级', type: '解锁', value: lvl => lvl >= 4},
        {name: '书籍升级', type: '解锁', value: lvl => lvl >= 5}
    ]},
    地方政府: {cap: 1, note: 'village_21', 保留: true, icon: 'mdi-city-variant', requirement() {
        return store.state.解锁.层 3 建筑.use;
    }, timeNeeded() {
        return buildNum(240, 'K');
    }, price() {
        return {植物纤维: buildNum(1.02, 'M'), 木头: buildNum(975, 'K'), 玻璃: buildNum(16, 'K'), 金币: buildNum(280, 'K')};
    }, effect: [
        {name: '层 4 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    公寓: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-home-city', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.65, lvl) * buildNum(20, 'M'),
            玻璃: Math.pow(1.65, lvl) * buildNum(29.5, 'K'),
            硬木: Math.pow(1.3, lvl) * 1500,
            宝石: Math.pow(1.35, lvl) * 600
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * 7200);
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl * 2}
    ]},
    寺庙: {cap: 30, icon: 'mdi-temple-hindu', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            玻璃: Math.pow(1.25, lvl) * 8000,
            水: Math.pow(1.5, lvl) * buildNum(2, 'M'),
            金币: Math.pow(1.45, lvl) * buildNum(100, 'K'),
            知识: 15 * lvl + 125,
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(62.5, 'K'));
    }, effect: [
        {name: '信仰容量', type: '*', value: lvl => Math.pow(1.2, lvl)}
    ]},
    方尖碑: {cap: 0, capMult: true, icon: 'mdi-tower-fire', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {金币: Math.pow(4.5, lvl) * buildNum(50, 'K')};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.45, lvl) * buildNum(50, 'K'));
    }, effect: [
        {name: '金币容量', type: '*', value: lvl => Math.pow(2, lvl)},
        {name: '材料容量', type: '*', value: lvl => Math.pow(1.2, lvl)}
    ]},
    供奉底座: {cap: 4, hasDescription: true, note: 'village_23', icon: 'mdi-table-furniture', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(3, lvl) * buildNum(32.5, 'K'));
    }, price(lvl) {
        return [
            {植物纤维: buildNum(2, 'M'), 木头: buildNum(2, 'M'), 石头: buildNum(2, 'M')},
            {金币: buildNum(10, 'M'), 金属: buildNum(3, 'M'), 水: buildNum(5, 'M')},
            {玻璃: buildNum(120, 'K'), 硬木: buildNum(40, 'K'), 宝石: buildNum(40, 'K')},
            {知识: 600, 科学: 200, 快乐: 750}
        ][lvl];
    }, effect: [
        {name: '供品', type: '解锁', value: lvl => lvl >= 1},
        {name: '层 2 供品', type: '解锁', value: lvl => lvl >= 2},
        {name: '层 3 供品', type: '解锁', value: lvl => lvl >= 3},
        {name: '层 4 供品', type: '解锁', value: lvl => lvl >= 4}
    ]},
    剧院: {cap: 5, capMult: true, note: 'village_24', subtype: '这座建筑算作一个工作站', icon: 'mdi-drama-masks', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        let obj = {石头: Math.pow(2.15, lvl) * buildNum(3, 'M'), 玻璃: Math.pow(1.8, lvl) * buildNum(14.8, 'K')};
        if (lvl >= 1) {
            obj.硬木 = Math.pow(1.75, lvl - 1) * 2000;
        }
        if (lvl >= 2) {
            obj.宝石 = Math.pow(1.75, lvl - 2) * 2750;
        }
        return obj;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(60, 'K'));
    }, effect: [
        {name: 'entertainer', type: 'villageJob', value: lvl => lvl}
    ]},
    伐木工小屋: {cap: 10, capMult: true, note: 'village_25', subtype: '这座建筑算作一个工作站', icon: 'mdi-axe', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {植物纤维: Math.pow(1.85, lvl) * buildNum(7.7, 'M'), 金属: Math.pow(1.85, lvl) * buildNum(1.35, 'M')};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(10, 'K'));
    }, effect: [
        {name: 'lumberjack', type: 'villageJob', value: lvl => lvl},
        {name: '硬木容量', type: '+', value: lvl => lvl > 1 ? (200 * (lvl - 1)) : null}
    ]},
    深矿井: {cap: 10, capMult: true, note: 'village_26', subtype: '这座建筑算作一个工作站', icon: 'mdi-tunnel', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.85, lvl) * buildNum(13, 'M'),
            知识: lvl * 10 + 165,
            硬木: Math.pow(1.65, lvl) * 500
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(12, 'K'));
    }, effect: [
        {name: 'blastMiner', type: 'villageJob', value: lvl => lvl},
        {name: '宝石容量', type: '+', value: lvl => lvl > 1 ? (200 * (lvl - 1)) : null}
    ]},
    大存储空间: {cap: 20, capMult: true, icon: 'mdi-database-settings', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {硬木: Math.pow(lvl * 0.03 + 1.25, lvl) * 900, 宝石: Math.pow(lvl * 0.03 + 1.25, lvl) * 900};
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.15, lvl) * buildNum(15, 'K'));
    }, effect: [
        {name: '水容量', type: '*', value: lvl => splicedPowLinear(1.25, 0.2, 20, lvl)},
        {name: '硬木容量', type: '*', value: lvl => splicedPowLinear(1.2, 0.1, 20, lvl)},
        {name: '宝石容量', type: '*', value: lvl => splicedPowLinear(1.2, 0.1, 20, lvl)}
    ]},
    豪宅: {cap: 25, capMult: true, note: 'village_27', subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-bank', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            金属: Math.pow(1.65, lvl) * buildNum(7, 'M'),
            硬木: Math.pow(1.35, lvl) * 4000,
            宝石: Math.pow(1.3, lvl) * 9200,
            金币: Math.pow(2.15, lvl) * buildNum(25, 'M')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(18, 'K'));
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl},
        {name: '幸福', type: '+', value: lvl => lvl * 0.002}
    ]},
    湖泊: {cap: 10, capMult: true, note: 'village_28', subtype: '这座建筑算作一个工作站', icon: 'mdi-waves', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            水: Math.pow(2.25, lvl) * buildNum(50, 'M'),
            玻璃: Math.pow(1.65, lvl) * buildNum(60, 'K'),
            宝石: Math.pow(1.65, lvl) * buildNum(11, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(20, 'K'));
    }, effect: [
        {name: 'fisherman', type: 'villageJob', value: lvl => lvl}
    ]},
    宝石锯片: {icon: 'mdi-saw-blade', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.15, lvl) * buildNum(30, 'K'));
    }, price(lvl) {
        return {石头: Math.pow(1.85, lvl) * buildNum(75, 'M'), 宝石: Math.ceil(Math.pow(1.5, lvl) * buildNum(15, 'K'))};
    }, effect: [
        {name: '建造速度', type: '*', value: lvl => Math.pow(1.2, lvl)}
    ]},
    微型玻璃制造厂: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-glass-tulip', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.5, lvl) * buildNum(125, 'M'),
            水: Math.pow(1.85, lvl) * buildNum(120, 'M'),
            硬木: Math.pow(1.3, lvl) * buildNum(12.5, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(14, 'K'));
    }, effect: [
        {name: '玻璃增益', type: '*', value: lvl => lvl * 0.1 + 1},
        {name: '工人', type: '+', value: lvl => lvl > 4 ? Math.floor(lvl / 5) : null}
    ]},
    丢失的页面: {icon: 'mdi-script-text', cap: 10, note: 'village_29', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.5, lvl) * buildNum(80, 'K'));
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.85, lvl) * buildNum(185, 'M'),
            木头: Math.pow(1.85, lvl) * buildNum(140, 'M'),
            知识: lvl * 15 + 220,
            硬木: Math.pow(1.65, lvl) * buildNum(22, 'K')
        };
    }, effect: [
        {name: '知识容量', type: '+', value: lvl => lvl * 8},
        {name: '信仰容量', type: '+', value: lvl => lvl * 20},
        {name: '斧子升级', type: '解锁', value: lvl => lvl >= 2},
        {name: '炸弹升级', type: '解锁', value: lvl => lvl >= 4},
        {name: '收费升级', type: '解锁', value: lvl => lvl >= 6},
        {name: '鱼竿升级', type: '解锁', value: lvl => lvl >= 8},
        {name: '圣书升级', type: '解锁', value: lvl => lvl >= 10}
    ]},
    操场: {cap: 5, note: 'village_30', icon: 'mdi-slide', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.15, lvl) * buildNum(140, 'K'));
    }, price(lvl) {
        return {水: Math.pow(4, lvl) * buildNum(250, 'M'), 金币: Math.pow(3, lvl) * buildNum(50, 'M')};
    }, effect: [
        {name: '幸福', type: '+', value: lvl => lvl * 0.01}
    ]},
    政府: {cap: 1, 保留: true, icon: 'mdi-city', requirement() {
        return store.state.解锁.层 4 建筑.use;
    }, timeNeeded() {
        return buildNum(1.5, 'M');
    }, price() {
        return {硬木: buildNum(50, 'K'), 宝石: buildNum(50, 'K'), 金币: buildNum(150, 'M'), 知识: 260};
    }, effect: [
        {name: '层 5 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    现代住宅: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-home-modern', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.65, lvl) * buildNum(330, 'M'),
            玻璃: Math.pow(1.65, lvl) * buildNum(240, 'K'),
            硬木: Math.pow(1.3, lvl) * buildNum(77.5, 'K'),
            宝石: Math.pow(1.35, lvl) * buildNum(18, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(200, 'K'));
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl * 3}
    ]},
    喷泉: {cap: 10, icon: 'mdi-fountain', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(275, 'K'));
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.65, lvl) * buildNum(1.7, 'B'),
            石头: Math.pow(1.65, lvl) * buildNum(1.35, 'B'),
            金属: Math.pow(1.65, lvl) * buildNum(290, 'M'),
            金币: Math.pow(1.85, lvl) * buildNum(650, 'M')
        };
    }, effect: [
        {name: '水增益', type: '*', value: lvl => Math.pow(1.4, lvl)},
        {name: '水容量', type: '*', value: lvl => Math.pow(2, lvl)}
    ]},
    实验室: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-flask', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            金属: Math.pow(1.85, lvl) * buildNum(70, 'M'),
            玻璃: Math.pow(1.85, lvl) * buildNum(475, 'K'),
            宝石: Math.pow(1.85, lvl) * buildNum(140, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(300, 'K'));
    }, effect: [
        {name: 'scientist', type: 'villageJob', value: lvl => lvl},
        {name: '科学容量', type: '+', value: lvl => lvl > 2 ? ((lvl - 2) * 5) : null},
        {name: '突破升级', type: '解锁', value: lvl => lvl >= 2}
    ]},
    法庭: {cap: 2, hasDescription: true, icon: 'mdi-gavel', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            硬木: Math.pow(1.85, lvl) * buildNum(280, 'K'),
            知识: Math.round(Math.pow(1.15, lvl) * 290),
            科学: lvl * 20 + 30
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.5, lvl) * buildNum(480, 'K'));
    }, effect: [
        {name: '税收政策', type: '+', value: lvl => lvl >= 1 ? 1 : null},
        {name: '移民政策', type: '+', value: lvl => lvl >= 2 ? 1 : null}
    ]},
    温室: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-greenhouse', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.85, lvl) * buildNum(1.15, 'B'),
            玻璃: Math.pow(1.85, lvl) * buildNum(900, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(550, 'K'));
    }, effect: [
        {name: 'gardener', type: 'villageJob', value: lvl => lvl}
    ]},
    满篮: {cap: 8, icon: 'mdi-basket', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(2.4, lvl) * buildNum(2.4, 'B'),
            快乐: Math.ceil(Math.pow(1.35, lvl) * 70)
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.75, lvl) * buildNum(1.5, 'M'));
    }, effect: [
        {name: '食物增益', type: '*', value: lvl => Math.pow(1.2, lvl) * (lvl * 0.25 + 1)},
        {name: '信仰容量', type: '+', value: lvl => lvl * 32}
    ]},
    储藏室: {cap: 20, icon: 'mdi-warehouse', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.85, lvl) * buildNum(4.5, 'B'),
            金属: Math.pow(1.65, lvl) * buildNum(360, 'M'),
            硬木: Math.pow(1.5, lvl) * buildNum(575, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(700, 'K'));
    }, effect: [
        {name: '基础材料容量', type: '*', value: lvl => Math.pow(1.5, lvl)},
        {name: '科学容量', type: '+', value: lvl => lvl * 8}
    ]},
    生物实验室: {cap: 5, icon: 'mdi-dna', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            金属: Math.pow(2.3, lvl) * buildNum(580, 'M'),
            宝石: Math.pow(1.85, lvl) * buildNum(695, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.75, lvl) * buildNum(1, 'M'));
    }, effect: [
        {name: '玻璃容量', type: '*', value: lvl => Math.pow(1.5, lvl)},
        {name: '改造植物升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '多巴胺升级', type: '解锁', value: lvl => lvl >= 3},
        {name: '肾上腺素升级', type: '解锁', value: lvl => lvl >= 5}
    ]},
    税务局: {cap: 3, icon: 'mdi-office-building', requirement() {
        return store.state.解锁.层 5 建筑.use && store.state.upgrade.item.法庭.level >= 1;
    }, price(lvl) {
        return {
            石头: Math.pow(6, lvl) * buildNum(10.5, 'B'),
            水: Math.pow(15, lvl) * buildNum(75, 'B'),
            知识: lvl * 75 + 350,
            金币: Math.pow(3.5, lvl) * buildNum(6, 'B')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(2.5, lvl) * buildNum(1.2, 'M'));
    }, effect: [
        {name: '税收政策', type: '+', value: lvl => lvl}
    ]},
    节日: {icon: 'mdi-party-popper', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            快乐: Math.ceil(Math.pow(1.15, lvl) * 100)
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.35, lvl) * buildNum(750, 'K'));
    }, effect: [
        {name: '幸福', type: '+', value: lvl => lvl * 0.003},
        {name: '税率', type: '*', value: lvl => lvl * 0.05 + 1}
    ]},
    墓地: {cap: 10, icon: 'mdi-grave-stone', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(lvl * 0.1 + 1.85, lvl) * buildNum(20, 'B'),
            石头: Math.pow(lvl * 0.1 + 1.85, lvl) * buildNum(27.5, 'B')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(1.5, 'M'));
    }, effect: [
        {name: '供品力量', type: '*', value: lvl => lvl * 0.4 + 1},
        {name: '信仰容量', type: '+', value: lvl => lvl * 32}
    ]},
    清真寺: {cap: 25, icon: 'mdi-mosque', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            石头: Math.pow(2.12, lvl) * buildNum(155, 'B'),
            玻璃: Math.pow(1.9, lvl) * buildNum(40, 'M'),
            宝石: Math.pow(1.55, lvl) * buildNum(17, 'M')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(3.2, 'M'));
    }, effect: [
        {name: '信仰增益', type: '+', value: lvl => getSequence(2, lvl)}
    ]},
    水塔: {cap: 12, icon: 'mdi-tower-beach', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(2.45, lvl) * buildNum(260, 'B'),
            知识: lvl * 125 + 700
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.65, lvl) * buildNum(8, 'M'));
    }, effect: [
        {name: '水增益', type: '*', value: lvl => Math.pow(1.2, lvl) * (lvl * 0.5 + 1)}
    ]},
    室外泵: {cap: 5, icon: 'mdi-water-pump', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            水: Math.pow(3.3, lvl) * buildNum(1.6, 'T'),
            快乐: lvl * 180 + 720
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.65, lvl) * buildNum(18.5, 'M'));
    }, effect: [
        {name: '植物纤维增益', type: '*', value: lvl => lvl * 0.4 + 1}
    ]},
    银行金库: {cap: 12, icon: 'mdi-safe-square', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, price(lvl) {
        return {
            金属: Math.pow(1.85, lvl) * buildNum(7.35, 'B'),
            科学: lvl * 45 + 270
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.65, lvl) * buildNum(40, 'M'));
    }, effect: [
        {name: '金币容量', type: '*', value: lvl => Math.pow(1.6, lvl)}
    ]},
    蒸汽机: {cap: 1, 保留: true, icon: 'mdi-turbine', requirement() {
        return store.state.解锁.层 5 建筑.use;
    }, timeNeeded() {
        return buildNum(600, 'M');
    }, price() {
        return {
            金属: buildNum(27.6, 'B'),
            水: buildNum(45, 'T'),
            硬木: buildNum(175, 'M'),
            金币: buildNum(3.5, 'T'),
            科学: 440,
            快乐: 1500
        };
    }, effect: [
        {name: '层 6 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    高级公寓: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-balcony', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.65, lvl) * buildNum(63, 'T'),
            大理石: Math.pow(1.35, lvl) * 600
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(140, 'M'));
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl * 4}
    ]},
    石油钻机: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-tower-fire', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            石头: Math.pow(2.35, lvl) * buildNum(1.32, 'T'),
            水: Math.pow(4.1, lvl) * buildNum(90, 'T'),
            知识: lvl * 500 + 1500
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(320, 'M'));
    }, effect: [
        {name: 'oilWorker', type: 'villageJob', value: lvl => lvl},
        {name: '油容量', type: '+', value: lvl => lvl > 1 ? ((lvl - 1) * 400) : null}
    ]},
    发电机: {hasDescription: true, icon: 'mdi-engine', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.45, lvl) * buildNum(1.93, 'T'),
            金属: Math.pow(1.2, lvl) * buildNum(84, 'B'),
            油: Math.pow(1.25, lvl) * 1400
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.2, lvl) * buildNum(550, 'M'));
    }, effect: [
        {name: '力量', type: '+', value: lvl => lvl * 3},
        {name: '污染', type: '+', value: lvl => lvl * 2}
    ]},
    灯塔: {cap: 25, icon: 'mdi-lighthouse', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(lvl * 0.06 + 1.6, lvl) * buildNum(3.68, 'T'),
            宝石: Math.pow(lvl * 0.04 + 1.4, lvl) * buildNum(480, 'M'),
            油: Math.pow(lvl * 0.05 + 1.5, lvl) * 2800
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(650, 'M'));
    }, effect: [
        {name: '信仰增益', type: '*', value: lvl => Math.pow(1.225, lvl)}
    ]},
    大堂: {icon: 'mdi-account-group', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            金币: Math.pow(lvl * 0.05 + 1.35, lvl) * buildNum(13.5, 'T')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(750, 'M'));
    }, effect: [
        {name: '污染耐受性', type: '+', value: lvl => lvl}
    ]},
    油存储: {cap: 20, icon: 'mdi-barrel', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            硬木: Math.pow(1.55, lvl) * buildNum(1.05, 'B'),
            玻璃: Math.pow(1.6, lvl) * buildNum(2.25, 'B')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(1.1, 'B'));
    }, effect: [
        {name: '金属增益', type: '*', value: lvl => Math.pow(1.1, lvl)},
        {name: '金属容量', type: '*', value: lvl => Math.pow(1.4, lvl)},
        {name: '油容量', type: '*', value: lvl => Math.pow(1.4, lvl)}
    ]},
    美术馆: {cap: 10, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-palette-advanced', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(2.9, lvl) * buildNum(198, 'T'),
            油: Math.pow(2.2, lvl) * buildNum(264, 'K'),
            快乐: lvl * 400 + 2200
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.33, lvl) * buildNum(1.6, 'B'));
    }, effect: [
        {name: 'sculptor', type: 'villageJob', value: lvl => lvl},
        {name: '大理石容量', type: '+', value: lvl => lvl > 1 ? ((lvl - 1) * 100) : null}
    ]},
    挖掘机: {icon: 'mdi-excavator', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            硬木: Math.pow(1.35, lvl) * buildNum(5.28, 'B'),
            油: Math.pow(1.6, lvl) * buildNum(360, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(2.2, 'B'));
    }, effect: [
        {name: '基础材料增益', type: '*', value: lvl => Math.pow(1.25, lvl) * (0.25 * lvl + 1)},
        {name: '污染', type: '+', value: lvl => lvl}
    ]},
    油车: {icon: 'mdi-tanker-truck', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            宝石: Math.pow(1.45, lvl) * buildNum(7.8, 'B')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(3, 'B'));
    }, effect: [
        {name: '油容量', type: '*', value: lvl => Math.pow(2, lvl)},
        {name: '污染', type: '+', value: lvl => lvl}
    ]},
    老图书馆: {icon: 'mdi-book-clock', cap: 2, requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(7.1, lvl) * buildNum(1.68, 'Qa'),
            大理石: Math.pow(4.5, lvl) * 7500,
            知识: lvl * 600 + 2800
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(10, lvl) * buildNum(4, 'B'));
    }, effect: [
        {name: '科学容量', type: '+', value: lvl => lvl * 20},
        {name: '喷头升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '贪婪升级', type: '解锁', value: lvl => lvl >= 2}
    ]},
    移民局: {cap: 3, icon: 'mdi-office-building', requirement() {
        return store.state.解锁.层 6 建筑.use && store.state.upgrade.item.法庭.level >= 2;
    }, price(lvl) {
        return {
            知识: lvl * 2000 + 4500,
            科学: lvl * 750 + 1500,
            金币: Math.pow(10, lvl) * buildNum(1.5, 'Qa')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(8, lvl) * buildNum(5, 'B'));
    }, effect: [
        {name: '移民政策', type: '+', value: lvl => lvl}
    ]},
    大理石雕像: {cap: 10, icon: 'mdi-human-female-dance', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            大理石: Math.pow(1.65, lvl) * 2250
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.75, lvl) * buildNum(120, 'B'));
    }, effect: [
        {name: '幸福', type: '+', value: lvl => lvl * 0.004},
        {name: '大理石容量', type: '*', value: lvl => Math.pow(1.2, lvl)},
        {name: '知识容量', type: '*', value: lvl => lvl * 0.1 + 1}
    ]},
    黑暗教派: {cap: 4, hasDescription: true, icon: 'mdi-pentagram', requirement() {
        return store.state.解锁.层 6 建筑.use && store.state.upgrade.item.法庭.level >= 2;
    }, price(lvl) {
        return {
            宝石: Math.pow(6.25, lvl) * buildNum(82, 'B'),
            油: Math.pow(5.5, lvl) * buildNum(55, 'M'),
            大理石: Math.pow(2.5, lvl) * buildNum(50, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(6, lvl) * buildNum(11, 'B'));
    }, effect: [
        {name: '宗教政策', type: '+', value: lvl => lvl}
    ]},
    屠宰场: {icon: 'mdi-cow', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, price(lvl) {
        return {
            植物纤维: Math.pow(1.5, lvl) * buildNum(15.4, 'Qa'),
            木头: Math.pow(1.5, lvl) * buildNum(12, 'Qa'),
            硬木: Math.pow(1.35, lvl) * buildNum(140, 'B'),
            水: Math.pow(1.9, lvl) * buildNum(9.5, 'Qi')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(16, 'B'));
    }, effect: [
        {name: '肉增益', type: '+', value: lvl => getSequence(4, lvl) * 100},
        {name: '污染', type: '+', value: lvl => lvl}
    ]},
    生态委员会: {cap: 1, 保留: true, icon: 'mdi-earth', requirement() {
        return store.state.解锁.层 6 建筑.use;
    }, timeNeeded() {
        return buildNum(250, 'B');
    }, price() {
        return {油: buildNum(96, 'M'), 大理石: buildNum(500, 'K'), 金币: buildNum(14, 'Qa'), 科学: 2000, 快乐: 6000};
    }, effect: [
        {name: '层 7 建筑', type: '解锁', value: lvl => lvl >= 1}
    ]},
    树屋: {cap: 25, capMult: true, subtype: '该建筑算作住房，可提高前 25 栋建筑的全局等级', icon: 'mdi-tree-outline', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            硬木: Math.pow(1.45, lvl) * buildNum(162.5, 'B'),
            常见战利品: Math.ceil(Math.pow(1.2, lvl) * 5)
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(45, 'B'));
    }, effect: [
        {name: '工人', type: '+', value: lvl => lvl * 6}
    ]},
    雨林: {icon: 'mdi-forest', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            硬木: Math.pow(1.35, lvl) * buildNum(185, 'B'),
            水: Math.pow(1.9, lvl) * buildNum(12.5, 'Qi')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(250, 'B'));
    }, effect: [
        {name: '污染', type: '+', value: lvl => lvl * -1}
    ]},
    豪华储物空间: {cap: 20, icon: 'mdi-warehouse', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            木头: Math.pow(1.85, lvl) * buildNum(121, 'Qa'),
            石头: Math.pow(1.85, lvl) * buildNum(143, 'Qa'),
            油: Math.pow(1.6, lvl) * buildNum(234, 'M')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.3, lvl) * buildNum(60, 'B'));
    }, effect: [
        {name: '金属容量', type: '*', value: lvl => Math.pow(1.3, lvl)},
        {name: '玻璃容量', type: '*', value: lvl => Math.pow(1.4, lvl)},
        {name: '奢侈材料容量', type: '*', value: lvl => Math.pow(1.25, lvl)},
    ]},
    金字塔: {cap: 10, hasDescription: true, capMult: true, subtype: '这座建筑算作一个工作站', icon: 'mdi-pyramid', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            石头: Math.pow(3.75, lvl) * buildNum(375, 'Qa'),
            大理石: Math.pow(2.45, lvl) * buildNum(2.85, 'M'),
            快乐: lvl * 4000 + buildNum(15.5, 'K')
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.45, lvl) * buildNum(100, 'B'));
    }, effect: [
        {name: 'explorer', type: 'villageJob', value: lvl => lvl},
        {name: '战利品品质', type: '+', value: lvl => lvl > 1 ? ((lvl - 1) * 5) : null}
    ]},
    奖杯柜: {cap: 6, icon: 'mdi-gradient-vertical', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        let obj = {};
        obj[`village_loot${ lvl }`] = 1;
        return obj;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(2.5, lvl) * buildNum(150, 'B'));
    }, effect: [
        {name: '材料增益', type: '*', value: lvl => Math.pow(1.4, lvl)},
        {name: '材料容量', type: '*', value: lvl => Math.pow(1.2, lvl)}
    ]},
    古玩: {icon: 'mdi-store', cap: 12, requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        let obj = {
            金币: Math.pow(2.5, lvl) * buildNum(32.5, 'Qa')
        };
        obj[`village_loot${ Math.floor(lvl / 2) }`] = Math.pow(10, lvl % 2) * 10;
        return obj;
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(3, lvl) * buildNum(200, 'B'));
    }, effect: [
        {name: '金币容量', type: '*', value: lvl => Math.pow(1.3, lvl)},
        {name: '野心升级', type: '解锁', value: lvl => lvl >= 1},
        {name: '认知升级', type: '解锁', value: lvl => lvl >= 2},
        {name: '好奇心升级', type: '解锁', value: lvl => lvl >= 3},
        {name: '崇拜升级', type: '解锁', value: lvl => lvl >= 4},
        {name: '易货升级', type: '解锁', value: lvl => lvl >= 5},
        {name: '火花升级', type: '解锁', value: lvl => lvl >= 6},
    ]},
    风力发电机: {cap: 20, icon: 'mdi-wind-turbine', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            金属: Math.pow(1.65, lvl) * buildNum(1.25, 'Qa'),
            罕见的战利品: Math.ceil(Math.pow(1.15, lvl) * (3 + lvl))
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(240, 'B'));
    }, effect: [
        {name: '力量', type: '+', value: lvl => lvl},
        {name: '战利品增益', type: '*', value: lvl => lvl * 0.05 + 1}
    ]},
    雷达: {cap: 10, hasDescription: true, icon: 'mdi-satellite-uplink', requirement() {
        return store.state.解锁.层 7 建筑.use && store.state.upgrade.item.法庭.level >= 2;
    }, price(lvl) {
        return {
            金属: Math.pow(3.15, lvl) * buildNum(6.57, 'Qa'),
            大理石: Math.pow(2.4, lvl) * buildNum(41.5, 'M'),
            科学: lvl * 650 + 3500
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(3, lvl) * buildNum(333, 'B'));
    }, effect: [
        {name: '扫描政策', type: '+', value: lvl => lvl}
    ]},
    水力涡轮: {cap: 20, icon: 'mdi-hydro-power', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            水: Math.pow(2.2, lvl) * buildNum(200, 'Qi'),
            玻璃: Math.pow(1.65, lvl) * buildNum(6.8, 'T'),
            稀有战利品: Math.ceil(Math.pow(1.14, lvl) * (2 + lvl))
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(420, 'B'));
    }, effect: [
        {name: '力量', type: '+', value: lvl => lvl},
        {name: '水增益', type: '*', value: lvl => Math.pow(1.15, lvl)}
    ]},
    太阳能板: {cap: 20, icon: 'mdi-solar-panel', requirement() {
        return store.state.解锁.层 7 建筑.use;
    }, price(lvl) {
        return {
            宝石: Math.pow(1.55, lvl) * buildNum(9, 'T'),
            油: Math.pow(1.8, lvl) * buildNum(30.4, 'B'),
            史诗战利品: Math.ceil(Math.pow(1.13, lvl) * (1 + lvl))
        };
    }, timeNeeded(lvl) {
        return Math.ceil(Math.pow(1.25, lvl) * buildNum(550, 'B'));
    }, effect: [
        {name: '力量', type: '+', value: lvl => lvl * 2}
    ]},
}